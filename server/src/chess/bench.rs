use ahash::AHashMap;
use std::borrow::Cow;
use std::collections::HashMap;
use std::collections::VecDeque;

// #[cfg(test)]
// mod bench_insert {
//     use super::*;
//     use test::Bencher;

//     fn create_data<T: std::iter::FromIterator<(i32, i32)>>(n: usize) -> T {

//     }
// }
